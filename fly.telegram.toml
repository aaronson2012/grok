app = 'grok-bot-telegram'
primary_region = 'iad'

[build]
  dockerfile = 'Dockerfile'

[env]
  DATABASE_PATH = '/data/grok.db'

[processes]
  app = "python bot_telegram.py"

[[mounts]]
  source = 'grok_data'
  destination = '/data'

[checks]
  [checks.alive]
    type = "machine"
    grace_period = "30s"
    interval = "60s"
    timeout = "10s"

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 512
